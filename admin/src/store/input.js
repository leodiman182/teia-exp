import { createStore } from 'vuex'

// Create a new store instance.
const store = createStore({
    state: {
        field: {},
        errorField: {}
    },
    getters: {},
    actions: {
    },
    mutations: {
    },
    modules: {},
})

export default store