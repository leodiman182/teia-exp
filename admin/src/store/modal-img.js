import { createStore } from 'vuex'

// Create a new store instance.
const store = createStore({
    state: {
        show: false,
        img: ''
    },
    getters: {},
    actions: {
    },
    mutations: {
    },
    modules: {},
})

export default store